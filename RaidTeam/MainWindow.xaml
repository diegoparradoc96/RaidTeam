<?xml version="1.0" encoding="utf-8"?>

<Window
    x:Class="RaidTeam.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="RaidTeam - WoW TBC Raid Manager">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.ColumnDefinitions>
            <!--Panel izquierdo para lista de jugadores-->
            <ColumnDefinition Width="300" />
            <!--Panel central para grupos de raid-->
            <ColumnDefinition Width="*" />
            <!--Panel derecho para clases de WoW TBC-->
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>

        <!--Panel Izquierdo - Lista de Jugadores-->
        <Border Grid.Column="0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Margin="10">
            <Grid Margin="20,20,20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <!-- Texto -->
                    <ColumnDefinition Width="Auto" />
                    <!-- Botón -->
                </Grid.ColumnDefinitions>

                <!-- Texto -->
                <TextBlock Text="Jugadores"
                Style="{ThemeResource SubtitleTextBlockStyle}"
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                Grid.Column="0" />

                <!-- Botón -->
                <Button Grid.Column="1"
                x:Name="AddPlayerButton"
                Content="+"
                Style="{ThemeResource AccentButtonStyle}"
                Width="40"
                Height="40"
                CornerRadius="20"
                FontSize="20"
                FontWeight="Bold"
                ToolTipService.ToolTip="Agregar nuevo jugador"
                Command="{Binding AddPlayerCommand}" />
            </Grid>
        </Border>

        <!--Panel Central - Placeholder para grupos (por ahora vac�o)-->
        <Border Grid.Column="1"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                CornerRadius="8"
                Margin="5,10,5,10">
            <TextBlock Text="Grupos de Raid (Pr�ximamente)"
                      Style="{ThemeResource SubtitleTextBlockStyle}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Opacity="0.6" />
        </Border>

        <!--Panel Derecho - Placeholder para clases (por ahora vac�o)-->
        <Border Grid.Column="2"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Margin="10">
            <TextBlock Text="Clases WoW TBC (Proximamente)"
                      Style="{ThemeResource CaptionTextBlockStyle}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Opacity="0.6"
                      TextWrapping="Wrap" />
        </Border>
    </Grid>
</Window>